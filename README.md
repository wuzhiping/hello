npx degit github:wuzhiping/hello/Clear Project

```
opencode run --thinking --continue \
        -m opencode/big-pickle --agent build \
          --log-level WARN --print-logs true \
                                    --dir ./ \
                            "完成一个计算器应用"
```
